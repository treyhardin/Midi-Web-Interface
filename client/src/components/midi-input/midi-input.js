// import { useMIDI } from "@react-midi/hooks/dist/use-midi";
import { useState } from "react";

export default function MIDIInput() {

  // const { inputs, outputs, hasMIDI } = useMIDI();

  // const [activeInputs, setActiveInputs] = useState(inputs)
  // const [activeOutputs, setActiveOutputs] = useState(outputs)

  // console.log(hasMIDI)
  // // if (inputs.length < 1) return <div>No MIDI Inputs</div>;
  // // return <div>MIDI Inputs</div>;

  // // console.log(inputs)

  // console.log(navigator.requestMIDIAccess)

  // return (
  //   <div className="midi-wrapper">
  //     <p>{hasMIDI ? "MIDI Supported" : "Your browser doesn't allow MIDI"}</p>
  //   </div>
  // )
}